jar {
	baseName = 'scores'
	version = '0.0.1-SNAPSHOT'
}

dependencies {
	compile project(':shared')
	compile 'org.springframework.boot:spring-boot-starter-data-jpa'
	runtime 'com.h2database:h2'
	testCompile project(':shared-test')
}
